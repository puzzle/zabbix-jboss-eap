diff -ur zabbix-3.2.3/src/zabbix_java/src/com/zabbix/gateway/JMXItemChecker.java zabbix-3.2.3-orig/src/zabbix_java/src/com/zabbix/gateway/JMXItemChecker.java
--- zabbix-3.2.3/src/zabbix_java/src/com/zabbix/gateway/JMXItemChecker.java	2017-01-22 20:52:56.000000000 +0100
+++ zabbix-3.2.3-orig/src/zabbix_java/src/com/zabbix/gateway/JMXItemChecker.java	2016-12-21 09:08:40.000000000 +0100
@@ -55,11 +55,7 @@
 			String conn = request.getString(JSON_TAG_CONN);
 			int port = request.getInt(JSON_TAG_PORT);
 
-			if (conn.startsWith("_")) {
-				url = new JMXServiceURL(conn.replaceAll("_([^.]+).", "$1:").replaceFirst("(.*):", "$1://") + ":" + port);
-			} else {
-				url = new JMXServiceURL("service:jmx:rmi:///jndi/rmi://[" + conn + "]:" + port + "/jmxrmi");
-			}
+			url = new JMXServiceURL("service:jmx:rmi:///jndi/rmi://[" + conn + "]:" + port + "/jmxrmi");
 			jmxc = null;
 			mbsc = null;
 
